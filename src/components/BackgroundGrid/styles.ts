import styled, { css } from 'styled-components'

export const Wrapper = styled.div`
  ${({ theme }) => css`
    top: 0;
    width: 300%;
    height: 100vh;
    position: absolute;

    > svg {
    }
  `}
`
